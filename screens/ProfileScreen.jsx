import ScreenWrapper from "../ui/ScreenWrapper";
import Title from "../ui/Title";

const ProfileScreen = () => {
  return (
    <ScreenWrapper>
      <Title>Profile</Title>
    </ScreenWrapper>
  );
};

export default ProfileScreen;
